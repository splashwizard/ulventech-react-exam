import { makeStyles } from "@material-ui/styles";

export default makeStyles(theme => ({
  body: {
    padding: "16px"
  },
  form: {
    paddingTop: "24px"
  },
  btnContainer: {
    textAlign: 'center'
  },
  progress: {
    marginRight: "5px",
    color: "white"
  },
}));